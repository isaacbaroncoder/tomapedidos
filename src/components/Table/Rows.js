// Import modules
import React from 'react';

// Import styles
import './styles/Rows.css';

const Rows = (props)=>{
    return(
        <h2>
            {props.title}
        </h2>
    );
}

export default Rows;